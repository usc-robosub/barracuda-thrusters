services:
  barracuda-thrusters:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    devices:
      - /dev/i2c-1:/dev/i2c-1
